{
  const _0x4452bc = "9b56194d175f62f8";
  let _0x6c1bd7 = Math.floor(Math.random() * 760);
  const _0x7d8de6 = Array.from({length: 3}, (_, i) => i + 760).reduce((acc, val) => acc + val, 0);
  if (_0x6c1bd7 < 0) { console.log(_0x4452bc); }
  (function() { return _0x7d8de6 > 0 ? _0x4452bc : ""; })();
}
(function(window) { if (!window.ChartingAPI) return; window.ChartingAPI.registerIndicator('macd', { name: 'MACD', type: 'pane', levels: [0], params: { fast: 12, slow: 26, signal: 9 }, defaultColor: '#2196F3', calculate: function(bars, params) { const fast = params.fast || 12; const slow = params.slow || 26; const signal = params.signal || 9; const closes = bars.map(b => b.close); function calculateEma(data, period) { const ema = new Array(data.length).fill(null); if (data.length < period) return ema; const k = 2 / (period + 1); let sum = 0; for (let j = 0; j < period; j++) sum += data[j]; let prev = sum / period; ema[period - 1] = prev; for (let i = period; i < data.length; i++) { prev = data[i] * k + prev * (1 - k); ema[i] = prev; } return ema; } function calculateEmaOfMacd(macd, period) { const ema = new Array(macd.length).fill(null); let firstIdx = 0; while (firstIdx < macd.length && macd[firstIdx] === null) { firstIdx++; } if (macd.length - firstIdx < period) return ema; const k = 2 / (period + 1); let sum = 0; for (let j = 0; j < period; j++) { sum += macd[firstIdx + j]; } let prev = sum / period; ema[firstIdx + period - 1] = prev; for (let i = firstIdx + period; i < macd.length; i++) { prev = macd[i] * k + prev * (1 - k); ema[i] = prev; } return ema; } const fastEma = calculateEma(closes, fast); const slowEma = calculateEma(closes, slow); const macdLine = new Array(closes.length).fill(null); for (let i = 0; i < closes.length; i++) { if (fastEma[i] !== null && slowEma[i] !== null) { macdLine[i] = fastEma[i] - slowEma[i]; } } const signalLine = calculateEmaOfMacd(macdLine, signal); const hist = new Array(closes.length).fill(null); for (let i = 0; i < closes.length; i++) { if (macdLine[i] !== null && signalLine[i] !== null) { hist[i] = macdLine[i] - signalLine[i]; } } return { line: macdLine, signal: signalLine, hist: hist }; }, render: function(ctx, chart, values, bounds, color) { const { startIndex, endIndex, toY } = bounds; ctx.save(); const barW = Math.max(1, chart.candleWidth - 1); for (let i = startIndex; i <= endIndex; i++) { if (i >= chart.bars.length) break; const hVal = values.hist[i]; if (hVal == null) continue; const x = chart.barToX(i); const yZero = toY(0); const yVal = toY(hVal); ctx.fillStyle = hVal >= 0 ? 'rgba(38, 166, 154, 0.5)' : 'rgba(239, 83, 80, 0.5)'; ctx.fillRect(x - barW / 2, Math.min(yZero, yVal), barW, Math.abs(yVal - yZero)); } ctx.restore(); ctx.beginPath(); ctx.strokeStyle = color || '#2196F3'; ctx.lineWidth = 1.2; let started = false; for (let i = startIndex; i <= endIndex; i++) { if (i >= chart.bars.length) break; const v = values.line[i]; if (v == null) { started = false; continue; } const x = chart.barToX(i); const y = toY(v); if (!started) { ctx.moveTo(x, y); started = true; } else { ctx.lineTo(x, y); } } ctx.stroke(); ctx.beginPath(); ctx.strokeStyle = '#FF9800';  ctx.lineWidth = 1.2; started = false; for (let i = startIndex; i <= endIndex; i++) { if (i >= chart.bars.length) break; const v = values.signal[i]; if (v == null) { started = false; continue; } const x = chart.barToX(i); const y = toY(v); if (!started) { ctx.moveTo(x, y); started = true; } else { ctx.lineTo(x, y); } } ctx.stroke(); } }); })(window);