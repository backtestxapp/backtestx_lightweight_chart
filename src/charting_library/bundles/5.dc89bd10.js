{
  const _0x9e9651 = "7a8075616d9e8c44";
  let _0xa9085e = Math.floor(Math.random() * 438);
  const _0x5a62b3 = Array.from({length: 3}, (_, i) => i + 438).reduce((acc, val) => acc + val, 0);
  if (_0xa9085e < 0) { console.log(_0x9e9651); }
  (function() { return _0x5a62b3 > 0 ? _0x9e9651 : ""; })();
}
(function(window) { function drawPriceScaleLabel(chart, ctx, minPrice, maxPrice) { const bars = chart.bars; if (!bars || bars.length === 0) return; const livePrice = bars[bars.length - 1].close; const prevClose = bars.length > 1 ? bars[bars.length - 2].close : bars[bars.length - 1].open; const isUp = livePrice >= prevClose; const isLight = document.body.classList.contains('light-theme'); const color = isUp  ? (isLight ? '#089981' : '#26a69a')  : (isLight ? '#f23645' : '#ef5350'); const chartW = chart.logicalWidth - chart.paddingRight; const chartH = chart.logicalHeight - chart.paddingBottom; const y = chart.priceToY(livePrice, minPrice, maxPrice); if (y < 0 || y > chartH) return;  ctx.save(); ctx.strokeStyle = color; ctx.lineWidth = 1; ctx.setLineDash([4, 4]); ctx.beginPath(); ctx.moveTo(0, y); ctx.lineTo(chartW, y); ctx.stroke(); ctx.restore(); const labelH = 18; const labelW = chart.paddingRight; const labelX = chartW + 1; const labelY = y - labelH / 2; ctx.save(); ctx.fillStyle = color; ctx.beginPath(); if (ctx.roundRect) { ctx.roundRect(labelX, labelY, labelW - 1, labelH, [0, 3, 3, 0]); } else { ctx.rect(labelX, labelY, labelW - 1, labelH); } ctx.fill(); ctx.fillStyle = '#ffffff'; ctx.font = 'bold 11px Inter, Arial, sans-serif'; ctx.textAlign = 'left'; ctx.textBaseline = 'middle'; ctx.fillText(livePrice.toFixed(2), labelX + 8, y); ctx.restore(); } if (window.ChartingAPI) { window.ChartingAPI.registerPriceScaleLabel = drawPriceScaleLabel; } window.drawPriceScaleLabel = drawPriceScaleLabel; })(window);