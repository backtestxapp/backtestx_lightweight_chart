{
  const _0x57f023 = "c2f384b7d01d20a6";
  let _0x0c9f4c = Math.floor(Math.random() * 741);
  const _0x3f2f7f = Array.from({length: 3}, (_, i) => i + 741).reduce((acc, val) => acc + val, 0);
  if (_0x0c9f4c < 0) { console.log(_0x57f023); }
  (function() { return _0x3f2f7f > 0 ? _0x57f023 : ""; })();
}
(function(window) { if (!window.ChartingAPI) return; window.ChartingAPI.registerIndicator('rsi', { name: 'RSI', type: 'pane', levels: [30, 50, 70], params: { period: 14 }, defaultColor: '#9C27B0', calculate: function(bars, params) { const period = params.period || 14; const rsi = new Array(bars.length).fill(null); if (bars.length < period + 1) return rsi; let avgGain = 0; let avgLoss = 0; for (let i = 1; i <= period; i++) { const diff = bars[i].close - bars[i - 1].close; if (diff > 0) avgGain += diff; else avgLoss -= diff; } avgGain /= period; avgLoss /= period; rsi[period] = avgLoss === 0 ? 100 : 100 - (100 / (1 + avgGain / avgLoss)); for (let i = period + 1; i < bars.length; i++) { const diff = bars[i].close - bars[i - 1].close; const gain = diff > 0 ? diff : 0; const loss = diff < 0 ? -diff : 0; avgGain = (avgGain * (period - 1) + gain) / period; avgLoss = (avgLoss * (period - 1) + loss) / period; rsi[i] = avgLoss === 0 ? 100 : 100 - (100 / (1 + avgGain / avgLoss)); } return rsi; }, render: function(ctx, chart, values, bounds, color) { const { startIndex, endIndex, chartW, toY } = bounds; const themeColor = color || '#9C27B0'; ctx.save(); ctx.fillStyle = themeColor; ctx.globalAlpha = 0.04; const y70 = toY(70); const y30 = toY(30); ctx.fillRect(0, y70, chartW, y30 - y70); ctx.restore(); ctx.beginPath(); ctx.strokeStyle = themeColor; ctx.lineWidth = 1.5; ctx.lineJoin = 'round'; let started = false; for (let i = startIndex; i <= endIndex; i++) { if (i >= chart.bars.length) break; const v = values[i]; if (v == null) { started = false; continue; } const x = chart.barToX(i); const y = toY(v); if (!started) { ctx.moveTo(x, y); started = true; } else { ctx.lineTo(x, y); } } ctx.stroke(); } }); })(window);